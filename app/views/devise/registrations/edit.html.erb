<h2>Bewerk <%= resource_name.to_s.humanize %></h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Wacht op antwoord voor: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password, autocomplete: "off", hint: "laat deze leeg wanneer u het wachtwoord niet wilt wijzigen", required: false %>
    <%= f.input :password_confirmation, required: false %>
    <%= f.input :current_password, hint: "om de wijzigingen door te voeren dient u het huidige wachtwoord mee te geven", required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Bijwerken", class: :button %>
  </div>
<% end %>

<h3>Account opheffen</h3>

<p>Ontevreden of niet langer meer actief? <%= link_to "Verwijder mijn account", registration_path(resource_name), data: { confirm: "Weet u zeker dat u uw account wilt verwijderen?" }, method: :delete %></p>

