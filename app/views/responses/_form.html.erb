<%= simple_form_for(@response) do |f| %>
  <%= f.error_notification %>

  <h1><%=@item.instruction%></h1>
  <h3 class="subtitle"><%=@item.item_stem%></h3>
  <div class="form-inputs">
    <%= f.hidden_field :item_id %>
    <%= f.input :value %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
